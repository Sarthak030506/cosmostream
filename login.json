{
  "query": "mutation Login($email: String!, $password: String!) { login(email: $email, password: $password) { token user { email role } } }",
  "variables": {
    "email": "admin@cosmostream.com",
    "password": "password123"
  }
}
